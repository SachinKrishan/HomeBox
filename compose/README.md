# Compose applications

Each application gets its own directory, pinned image versions, an example environment file, health checks where available, persistent-volume documentation, and a tested backup/restore procedure.
